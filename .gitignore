# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env*.local
.env.bak

# ElevenLabs CLI — ID mapping files
agents.json
tools.json
tests.json

# Animal videos (large files, hosted on CDN in prod)
public/animal-videos/

# Claude worktrees
.claude/worktrees/

# Marketing plans (strategy — not for public repos)
MARKETING-PLAN.md

# Generated animal landing pages (built from src/data/animals.ts)
public/animals/

# Vercel
.vercel/
.vercel

# Wrangler (Cloudflare CLI state)
.wrangler/

# ElevenLabs agent/tool configs (managed via ElevenLabs CLI)
# Track only our project's configs; ignore sibling project configs pulled by the CLI
agent_configs/*
!agent_configs/Animal-Penpals.json
tool_configs/*
!tool_configs/select_animal.json
!tool_configs/send_letter.json
!tool_configs/go_to_mailbox.json
!tool_configs/write_text.json
!tool_configs/read_letter_aloud.json
!tool_configs/read_previous_letter.json

# Codex CLI workspace config (per-user, not shared)
.codex/

# Locally generated pipeline videos (uploaded to R2; not for git)
pipeline/generated-videos/

# Misc duplicates
public/logo-wide copy 2.png
